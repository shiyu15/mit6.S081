参数存放在a0-a2寄存器中（根据riscv calling convention应该是a0-a7，但本次只用到前三个），13储存在a2中

在红框处main函数调用f，编译器自动帮我们算好了f(8)+1的结果，因而实际上没有调用f

根据main里面的printf位置，printf函数在0x0630处

刚从printf返回时，ra中储存的应该是下一条指令的地址，为0x038

如果riscv是大端的而不是小端的，需要将i设置为 0x726c6400。不需要改变57616的值。

这个值是调用printf函数时a2的值，因此确实是printf把a2当成了第三个参数，输出了a2的值